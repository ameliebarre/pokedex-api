import * as chai from "chai";
import * as mocah from "mocha";
import * as express from "express";

const expect = chai.expect;

describe("Profile spec", () => {

    const expressApp = express();

    before(async() => {

    });
});
